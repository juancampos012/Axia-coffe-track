import AccountsClients from '@/modules/accounts/AccountsClients';

export const metadata = { title: 'Cuentas Clientes · Axia Coffee' };

export default function ClientsAccountsPage() {
  return <AccountsClients />;
}
